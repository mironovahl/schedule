export interface ITableColumns {
  title: string
  dataIndex: string
  key: string
  className: string
}

export interface IColumnsVisibility {
    date: boolean,
    type: boolean,
    name: boolean,
    description: boolean,
    url: boolean,
    place: boolean,
    comment: boolean,
}
